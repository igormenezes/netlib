<!DOCTYPE html>
<html lang="pt-br">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
        <title>NetLib</title>
        <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
        {% stylesheets 'css/*' %}
        <link rel="stylesheet" href="{{ asset_url }}" type="text/css">
        {% endstylesheets %}
        {% javascripts 'js/jquery.min.js' 'js/libraries.js' 'js/jquery.form.min.js' 'js/ajaxPadrao.js' 'js/configAjax.js' 'js/jquery-ui.min.js' 'js/analytics.js' 'js/classes/cadastro.js' %}
        <script type="text/javascript" src="{{ asset_url }}" /></script>
        {% endjavascripts %}{# empty Twig template #}
    <style>
        #contato_comentario{height: 200px; width: 500px; text-align: center;}
    </style>
    <script type="text/javascript">
        $(document).ready(function() {
            {% if completo %}
            $(function() {
                $("<div>Enviado código de verificação para seu e-mail!</div>").dialog({
                    autoOpen: true,
                    title: 'Mensagem',
                    buttons: {
                        "OK": function() {
                            $(this).dialog("close");
                        }
                    },
                    height: "auto",
                    width: "auto"
                });
            });
            {% endif %}
        });
    </script>    
</head>
<body class="orangenormal">
        {% if erro %}
    <aside id= "alerts" class="alerts-on">
        <p id="mensagemApelido"></p>
        <p id="mensagemEmail"></p>
        <p id="aviso">{{ form_errors(form.login) }}</p>
        <p id="erros">{{erro}}</p>		
    </aside>             
        {% endif %}
    <section class='login-big-box orangedark'>
        <h1>Recuperar minha senha</h1>
        <form class="loginform" action="" method="POST">
            <section>
               {{ form_widget(form.login) }}
            </section>
            <p>Enviaremos um código de recuperação para o seu e-mail</p>
            <button class="last"type="submit">Enviar</button><br />
            <p><a href='{{path('netlib_novasenha')}}'>Já tem o código? Clique aqui</a></p>
        </form>
    </scetion>
</body>
</html>
